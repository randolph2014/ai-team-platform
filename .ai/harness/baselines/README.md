# Harness Baselines

Baselines capture measurable quality floors. Phase 1 only defines the policy skeleton.

## Naming

- Use `baseline.<concern>.<metric>` for concrete baseline records.
- Use `baseline.policy.<name>` for policy documents.

## Policy

Baselines are raise-only by default. Lowering a baseline requires explicit human approval or review evidence.
